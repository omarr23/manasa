export default{
    "validation": {
        "en_rule": "accepts letters, numbers in English and special characters only",
        "ar_rule": "accepts letters, numbers in Arabic and special characters only",
        "decmail_rule": "accepts numbers and decmail only"

      }
}